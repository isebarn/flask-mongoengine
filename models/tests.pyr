# http://docs.python-requests.org/en/master/user/quickstart/

env_file = ''

###env
url = "http://127.0.0.1:5000/api/{}"
products = "products"
variants = "variants"
features = "features"
###env

post(url.format(features), json={
	"feature": "feature_1"	
}, name="feature_1")

post(url.format(features), json={
	"feature": "feature_2"	
}, name="feature_2")


post(url.format(variants), json={
	"variant": "variant 1",
	"features": [
		feature_1.json(),
		feature_2.json()
	]
}, name="variant_1")



post(url.format(products), json={
	"product": "product 1",
	"variants": [
		variant_1.json()
	]
})

